export class AuthResponse {
    access_token: string;
    expires_in: string;
    refresh_expires_in: string;
    refresh_token: string;
    token_type;
    session_state: string;
    scope: string;
}
